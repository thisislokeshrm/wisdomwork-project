/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    './src/**/*.{js,ts,jsx,tsx}',  // Ensure Tailwind is applied to all files in the `src` directory
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};
